{
  "_comment": "Copy this file to .credentials.json (gitignored) and fill in real values. Each top-level key is a Credential name a pipeline references, e.g. Send Email with \"credential\": \"smtp\". For Gmail, use an App Password (Google account > 2FA > App passwords), not your login password.",
  "smtp": {
    "host": "smtp.gmail.com",
    "port": 587,
    "username": "you@gmail.com",
    "password": "your-16-char-app-password",
    "from": "you@gmail.com",
    "tls": true
  }
}
